import { useContext } from 'react'

// Layout Context
//import { LayoutContext } from '../templates/content_layout'

/**
 * Get the state of the current content layout
 */
export function useLayout() {
  // const layout = useContext(LayoutContext)

  // return {
  //   navPeek: layout.peek,
  //   setNavPeek: layout.setPeek,
  //   navMode: layout.navMode,
  //   setNavMode: layout.setNavMode
  // }
}